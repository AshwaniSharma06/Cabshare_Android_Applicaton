<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cabshare">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBywzyEoxTlrEAb-fv_wiLQ7O-IoHEjsIM" />

        <service
            android:name=".service.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".auth.SignupActivity" android:label="Signup" />
        <activity android:name=".MainActivity" android:label="Home" />
        <activity android:name=".ui.CreateTripActivity" android:label="Create Trip" />
        <activity android:name=".ui.FindTripActivity" android:label="Find Trips" />
        <activity android:name=".ui.ChatActivity" android:label="Chat" />
        <activity android:name=".ui.ProfileActivity" android:label="Profile" />
        <activity android:name=".ui.SelectLocationActivity" android:label="Select Location" />
        <activity android:name=".ui.MyTripsActivity" android:label="My Bookings" />
        <activity android:name=".ui.TripDetailActivity" android:label="Trip Details" />
        
    </application>

</manifest>
